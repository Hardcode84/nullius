-- Scenario fixtures use cooperative play in one force. Alignment is a separate
-- startup mode that gives players separate factions and landing sites.
data.raw["bool-setting"]["nullius-alignment"].default_value = false
